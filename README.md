
  # IPV6 WebSite

  This is a code bundle for IPV6 WebSite. The original project is available at https://www.figma.com/design/TXcoyxTNAn1GywNHlLE1uR/IPV6-WebSite.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  